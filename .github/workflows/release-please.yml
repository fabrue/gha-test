on:
  push:
    branches:
      - main
name: release-please
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: create tag
        uses: rickstaa/action-create-tag@v1
        with:
          tag: "foobar-$(cat version.yml)"
          message: "Latest release"

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: foobar-$(cat version.yml)